name: campus_vibe
description: A social platform for university students across Pakistan

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.1
  firebase_auth: ^4.9.0
  cloud_firestore: ^4.9.1
  firebase_storage: ^11.2.6
  provider: ^6.0.5
  flutter_secure_storage: ^8.0.0
  image_picker: ^1.0.4
  cached_network_image: ^3.2.3
  timeago: ^3.5.0
  uuid: ^3.0.7
  dio: ^5.3.2
  shared_preferences: ^2.2.1
  connectivity_plus: ^4.0.2
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
